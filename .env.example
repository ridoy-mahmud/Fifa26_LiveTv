# ─── MongoDB Atlas (server-side only) ──────────────────────────────────────
# Copy this file to `.env` and fill in real values. On Vercel, set these in
# Project Settings → Environment Variables (Production + Preview).
MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster0.xxxxx.mongodb.net/?appName=Cluster0
MONGODB_DB=wc2026

# ─── Admin Authentication ────────────────────────────────────────────────────
# Admin panel uses Firebase Google Authentication (client-side only)
# Only mahamulhasan38@gmail.com can access the admin panel
# Firebase configuration is in src/components/admin/AdminAccess.client.tsx
