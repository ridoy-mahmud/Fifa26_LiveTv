# Dependencies
node_modules
npm-debug.log
yarn-error.log
package-lock.json
bun.lock

# Build outputs
dist-ssr
.nitro
.vinxi
# NOTE: do NOT ignore `.vercel` — it contains the build output (`config.json`,
# `static/`, and the serverless functions) that Vercel needs at runtime.
# `.gitignore` still excludes `.vercel` so the build artifacts aren't committed.

# Environment
.env
.env.*
.env.local
.env.production.local

# IDE
.vscode
.idea
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Testing
coverage
.nyc_output

# Misc
*.log
*.pid
*.seed
*.pid.lock

# Stream data
json/
*.m3u
playlists/
streams/
exported-channels*.json
stream-links*.json
live-links*.json

# Development
.puku
.tanstack